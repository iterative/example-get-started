{
  "name": "experiments-get-started",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "extensions": ["Iterative.dvc", "ms-python.python", "vsls-contrib.codetour"],
  "postCreateCommand": "pip3 install --user -r src/requirements.txt"
}
